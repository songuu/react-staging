// 模板类型和地址
const JavaScript = new Map([['default', '']])

const TypeScript = new Map([['default', '']])

const appTemplate = new Map([
  ['js', JavaScript],
  ['ts', TypeScript],
])

export default appTemplate
