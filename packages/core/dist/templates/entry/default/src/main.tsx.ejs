import { createRoot, Root } from 'react-dom/client';

import App from './app';

function renderRoot() {
  const root = createRoot(document.getElementById("root"));

  root.render(<App />);
}

renderRoot();