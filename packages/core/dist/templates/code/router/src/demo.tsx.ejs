import React from 'react'

const Demo = () => {
  const containerStyle = {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100vh',
  }

  return (
    <div style={containerStyle}>
      <h1 style={{ textAlign: 'center', margin: 'auto' }}>Hello World!
      </h1>
    </div>
  )
}

export default Demo
